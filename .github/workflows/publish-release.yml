name: Publish Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 2.3.0)'
        required: true
        type: string

env:
  VERSION: ${{ github.event.inputs.version }}

permissions:
  contents: write
  id-token: write

jobs:
  publish-release:
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Verify release is ready
        run: |
          if ! grep -qF "## [$VERSION]" CHANGELOG.md; then
            echo "::error::CHANGELOG.md has no entry for [$VERSION]. Merge the prepare-release PR to main before running this workflow."
            exit 1
          fi

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          cache: 'yarn'
          registry-url: 'https://registry.npmjs.org'

      - name: Update npm for OIDC support
        run: npm install -g npm@latest

      - run: yarn install --frozen-lockfile

      - run: yarn test

      - run: yarn build

      - run: yarn build:node

      - name: Publish to npm
        run: npm publish --provenance

      - name: Publish draft GitHub release and tag main
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release edit "$VERSION" \
            --draft=false \
            --target "$(git rev-parse HEAD)" \
            --latest

      - name: Slack notification
        run: |
          release_url="https://github.com/${{ github.repository }}/releases/tag/$VERSION"
          payload=$(jq -n \
            --arg text ":javascript: *Web SDK ${VERSION}* has been released. <${release_url}|View release notes>." \
            '{"text": $text}')
          curl -sS -X POST -H 'Content-type: application/json' --data "$payload" "${{ secrets.SLACK_WEBHOOK }}"

      - name: Slack failure notification
        if: failure()
        run: |
          run_url="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}/attempts/${{ github.run_attempt }}"
          payload=$(jq -n \
            --arg text ":alert: Web SDK release ${VERSION} failed (attempt ${{ github.run_attempt }}). Run: ${run_url}" \
            '{"text": $text}')
          curl -sS -X POST -H 'Content-type: application/json' --data "$payload" "${{ secrets.SLACK_WEBHOOK }}"
