export class CriteriaCompletionChecker {
  getMatchedCriteria(criteriaData: string, localStoredEventList: string) {
    console.error('criteriaData', criteriaData);
    console.error('localStoredEventList', localStoredEventList);
  }
}
